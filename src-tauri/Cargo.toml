[package]
name = "rust-nn-studio"
version = "0.1.0"
edition = "2021"
description = "Desktop UI for Rust Neural Networks"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rust_neural_networks = { path = ".." }
